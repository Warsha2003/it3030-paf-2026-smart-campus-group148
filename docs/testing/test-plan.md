# Test Plan

- Run backend unit and context tests with Maven.
- Start the backend and verify `/`, `/home`, and booking endpoints.
- Install frontend dependencies and verify the Vite workspace starts successfully.
