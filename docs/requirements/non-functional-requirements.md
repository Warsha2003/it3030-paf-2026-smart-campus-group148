# Non-Functional Requirements

- The backend should remain runnable with Maven.
- The frontend workspace should remain runnable with npm and Vite.
- The project structure should clearly separate backend, frontend, docs, and report assets.
- Package and folder organization should support future auth, notification, resource, and ticket modules.
